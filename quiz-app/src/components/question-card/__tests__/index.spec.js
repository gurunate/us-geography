import { render } from '@testing-library/react';

import Link from '..';

describe('Link', () => {
    describe('component', () => {
        it('should render', () => {
            render(<Link />);
        });
    });
});
