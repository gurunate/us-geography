// const DataSource = require('./dataSource');
const { resolvers } = require('./resolvers');
const { typeDef } = require('./typeDef');

module.exports = {
    // DataSource,
    resolvers,
    typeDef
};
